{
  "version": 2,
  "name": "med-cabinet",
  "builds": [{ "src": "med-cabinet/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
  "routes": [
      { "src": "/(.*)", "dest": "med-cabinet/$1" }
  ]
}